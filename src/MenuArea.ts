import { V2d } from "./Vector2D";

export class MenuQuad {
  // for the purpose of avoiding CSS
  height: number;
  width: number;
  onClick: Function;
  Position: V2d;

  // content???
  constructor(height: number, width: number, ) {

  }
}